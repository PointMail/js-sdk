{
  "name": "point-api",
  "version": "1.0.0",
  "description": "Javascript API for Point autocomplete",
  "main": "build/index.js",
  "scripts": {
    "test": "jest",
    "build": "rollup src/main.js --format umd --name 'point-api' --file build/index.js",
    "build:watch": "npm run build -- -w"
  },
  "author": "Alex Iansiti <alex@easyemail.ai>",
  "license": "ISC",
  "devDependencies": {
    "jest": "^23.0.1",
    "rollup": "^0.59.4"
  },
  "dependencies": {
    "axios": "^0.18.0"
  }
}
